<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lantu.sys.mapper.BookMapper">
    <select id="getBooks"  resultType="string">
        SELECT book_name FROM x_book
    </select>
    <select id="getTypex" resultType="String">
        select type from x_book
    </select>
</mapper>
